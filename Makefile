# Minimal Makefile to satisfy Eclipse/OMNeT++ auto-build steps.
# This project currently runs NED/INI-based simulations and has no C++ targets.

.PHONY: all clean

all:
	@echo "No C++ build target in this project. Skipping compilation."

clean:
	@echo "Nothing to clean."
